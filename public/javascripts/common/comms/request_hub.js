var __slice = [].slice;
define([
    'underscore',
    'appConfig',
    './sockets/request_hub',
    './sync/request_hub',
    './endpoints'
], function (_, config, socketRequest, syncRequest, endpoints) {
});