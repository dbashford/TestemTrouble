define(['backbone', 'jquery', 'underscore', 'community', 'spec/helpers/community_searches_data'], function(Backbone, $, _, Collection, TEST_DATA) {

  it("foo", function() {
    expect(true).to.be.true;
  })

  it("bar", function() {
    expect(true).to.be.true;
  })

  it("baz", function() {
    expect(true).to.be.true;
  })

  it("one", function() {
    expect(true).to.be.true;
  })

  it("two", function() {
    expect(true).to.be.true;
  })

});
