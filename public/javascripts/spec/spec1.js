define(['backbone', 'jquery', 'underscore', 'mysearches', 'comms/response_hub', 'spec/helpers/my_searches_data'], function(Backbone, $, _, Collection, DATA_HUB, TEST_DATA) {

  it("foo", function() {
    expect(true).to.be.true;
  })

  it("bar", function() {
    expect(true).to.be.true;
  })

  it("baz", function() {
    expect(true).to.be.true;
  })

  it("one", function() {
    expect(true).to.be.true;
  })

  it("two", function() {
    expect(true).to.be.true;
  })

});
