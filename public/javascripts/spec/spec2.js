define(['backbone', 'jquery', 'searches/search/models/search_query_model', 'searches/search/search_container'], function(Backbone, $, Model, View) {

  it("foo", function() {
    expect(true).to.be.true;
  })

  it("bar", function() {
    expect(true).to.be.true;
  })

  it("baz", function() {
    expect(true).to.be.true;
  })

  it("one", function() {
    expect(true).to.be.true;
  })

  it("two", function() {
    expect(true).to.be.true;
  })

});
