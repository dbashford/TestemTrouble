define(['backbone', 'jquery', 'searches/my_searches/my_searches_container'], function(Backbone, $, View) {

  it("foo", function() {
    expect(true).to.be.true;
  })

  it("bar", function() {
    expect(true).to.be.true;
  })

  it("baz", function() {
    expect(true).to.be.true;
  })

  it("one", function() {
    expect(true).to.be.true;
  })

  it("two", function() {
    expect(true).to.be.true;
  })
});
